# =============================================================================
# CYBERDUDEBIVASH(R) SENTINEL APEX
# requirements-ci-light.txt  --  LIGHTWEIGHT CI REQUIREMENTS
# Version: v148.0.0
#
# PURPOSE:
#   Installs ONLY packages required for CI pipelines that do NOT need:
#     - torch / transformers (2GB+ ML models)
#     - scipy / scikit-learn  (heavy numerical stack)
#     - Google API / OAuth    (only used by sentinel-blogger)
#     - Stripe                (only used by revenue-orchestrator)
#
#   Use this in: status-monitor, weekly-analyst-briefing, bughunter-*,
#                detection-engine, report-engine, nexus, convergence,
#                omnishield, arsenal, zerodayhunter, ai-threat-analyst,
#                ai-predictions, precognition-engine, sovereign-platform,
#                genesis-powerhouse workflows.
#
#   Use requirements.txt ONLY for sentinel-blogger (needs full ML stack).
#
# INSTALL:
#   pip install -r requirements-ci-light.txt
# =============================================================================

# --- CORE WEB & API ---
# v184.9 FIX: this file had drifted into a stale duplicate of the pre-fix
# root requirements.txt -- 84 pip-audit findings across 9 packages, live in
# 14 production CI workflows (see file header). Applying the same
# already-verified floor-bumps shipped in requirements.txt (v184.7/v184.8),
# not re-deriving them -- see that file's inline comments for full CVE
# rationale per package.
fastapi==0.141.1
starlette>=1.3.1              # transitive via fastapi -- floor pinned, see requirements.txt v184.7 note
uvicorn[standard]==0.52.4
pydantic==2.13.4
python-dotenv==1.2.3
python-multipart==0.0.32

# --- NETWORKING & INTEL INGESTION ---
requests==2.34.2
httpx>=0.27.0                # v185.1 FIX: was ==0.26.0, hard-conflicting with
                              # api/requirements.txt's floor -- see requirements.txt's matching note.
aiohttp>=3.14.3
feedparser==6.0.10
beautifulsoup4==4.12.3
urllib3>=2.7.0
charset_normalizer>=3.3.2,<4.0

# --- STIX / TAXII (threat intel formats) ---
stix2==3.0.1
stix2-validator==3.1.2
taxii2-client==2.3.0

# --- SECURITY & JWT ---
cryptography>=43.0.3
pyjwt[crypto]==2.13.0

# --- LIGHT NUMERICAL (no torch/transformers/scipy) ---
numpy==1.26.3

# --- PDF & REPORT GENERATION ---
reportlab==4.0.9
fpdf2==2.7.7

# --- MONITORING ---
psutil==5.9.8
prometheus-client==0.19.0
colorama==0.4.6
