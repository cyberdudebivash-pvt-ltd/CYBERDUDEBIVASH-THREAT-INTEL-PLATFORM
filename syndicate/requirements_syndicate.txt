# CYBERDUDEBIVASH SENTINEL SYNDICATION ENGINE — Dependencies
# All dependencies are free, no paid services required

# v185.0 FIX: >=2.31.0 allowed the resolver to keep a pre-existing pinned
# 2.32.3 (or older) install, which carries PYSEC-2026-{1872,2275} -- fixed
# in 2.33.0+, verified in requirements.txt's own v184.7 comment. Matching
# that already-audited pin here since this file isn't in the pip-audit CI
# gate's --requirement list and so wasn't covered by that fix.
requests==2.34.2
