# v184.9 FIX: this file (and its 8 identical siblings under
# platform/services/*/requirements.txt) carried 10 pip-audit findings each --
# starlette (9, transitive via fastapi==0.111.0) and protobuf (1, transitive
# via the pinned OpenTelemetry stack). fastapi/starlette/pydantic/OTel bumps
# reuse the exact set already verified in requirements.txt (v184.7/v184.8);
# prometheus-fastapi-instrumentator additionally bumped 7.0.0->8.1.0 because
# every 7.x release hard-caps starlette<1.0, blocking the CVE fix -- 8.1.0 is
# the first release requiring starlette>=1.0.0. Verified via a real
# (non-dry-run) pip install + pip-audit: 10->0 vulnerabilities.
fastapi==0.141.1
starlette>=1.3.1
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.13.4
pydantic-settings==2.3.3
structlog==24.2.0
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
prometheus-fastapi-instrumentator==8.1.0
redis==5.0.6
asyncpg==0.29.0
